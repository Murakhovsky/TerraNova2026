<?php
$h = static fn($value) => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$money = static function ($amount, $currency = 'USD', $period = 'total') {
    if ($amount === null || $amount === '') {
        return 'Ціна за запитом';
    }

    $suffix = $period === 'month' ? ' / міс.' : ($period === 'day' ? ' / день' : '');

    return number_format((float) $amount, 0, '.', ' ') . ' ' . $currency . $suffix;
};
$dealLabels = [
    'sale' => 'Продаж',
    'rent' => 'Оренда',
    'investment' => 'Інвестиція',
];
$property = $property ?? null;
$images = $images ?? [];
$features = $features ?? [];
?>

<header class="tn-header" data-header>
  <a class="tn-logo" href="<?php echo $this->url->get(); ?>" aria-label="Terra Nova CLUB">
    <span class="tn-logo__mark">TN</span>
    <span class="tn-logo__text"><strong>Terra Nova</strong><small>CLUB</small></span>
  </a>

  <button class="tn-menu-button" type="button" aria-expanded="false" aria-controls="site-menu" data-menu-button>
    <span></span><span></span><span></span>
  </button>

  <nav class="tn-nav" id="site-menu" aria-label="Головне меню">
    <a href="<?php echo $this->url->get(); ?>">Головна</a>
    <a href="<?php echo $this->url->get('property/catalog'); ?>">Каталог</a>
    <a href="#tour">3D</a>
    <a href="#lead">Заявка</a>
  </nav>

  <div class="tn-header-actions">
    <a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('property/catalog'); ?>">Назад до каталогу</a>
    <a class="tn-btn tn-btn--dark" href="#lead">Запит</a>
  </div>
</header>

<main class="tn-page">
  <?php if (!$property): ?>
    <section class="tn-empty-state tn-empty-state--page">
      <h1>Об'єкт не знайдено</h1>
      <p>Можливо, він ще на модерації або був знятий з публікації.</p>
      <a class="tn-btn tn-btn--dark" href="<?php echo $this->url->get('property/catalog'); ?>">Перейти в каталог</a>
    </section>
  <?php else: ?>
    <?php
      $cover = $images[0]['image_url'] ?? 'https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=1400&q=80';
    ?>
    <section class="tn-property-hero">
      <div class="tn-property-hero__copy">
        <p class="tn-kicker"><?php echo $h($property['public_id']); ?> / <?php echo $h($dealLabels[$property['deal_type']] ?? $property['deal_type']); ?></p>
        <h1><?php echo $h($property['title']); ?></h1>
        <p><?php echo $h($property['short_description']); ?></p>
        <div class="tn-property-hero__price"><?php echo $h($money($property['price_amount'], $property['price_currency'], $property['price_period'])); ?></div>
        <div class="tn-card-tags">
          <span><?php echo $h($property['type_name']); ?></span>
          <span><?php echo $h($property['city']); ?></span>
          <?php if ((int) $property['has_3d_tour'] === 1): ?><span>3D tour</span><?php endif; ?>
        </div>
      </div>

      <div class="tn-gallery">
        <img class="tn-gallery__main" src="<?php echo $h($cover); ?>" alt="<?php echo $h($property['title']); ?>">
        <?php if (count($images) > 1): ?>
          <div class="tn-gallery__thumbs">
            <?php foreach (array_slice($images, 1, 4) as $image): ?>
              <img src="<?php echo $h($image['image_url']); ?>" alt="<?php echo $h($image['alt_text'] ?: $property['title']); ?>">
            <?php endforeach; ?>
          </div>
        <?php endif; ?>
      </div>
    </section>

    <section class="tn-property-detail-layout">
      <article class="tn-property-detail">
        <div class="tn-detail-grid">
          <?php if ($property['area_total']): ?><div><span>Площа</span><strong><?php echo $h((float) $property['area_total']); ?> м²</strong></div><?php endif; ?>
          <?php if ($property['land_area']): ?><div><span>Ділянка</span><strong><?php echo $h((float) $property['land_area']); ?> сот.</strong></div><?php endif; ?>
          <?php if ($property['rooms']): ?><div><span>Кімнати</span><strong><?php echo $h((float) $property['rooms']); ?></strong></div><?php endif; ?>
          <?php if ($property['bedrooms']): ?><div><span>Спальні</span><strong><?php echo $h($property['bedrooms']); ?></strong></div><?php endif; ?>
          <?php if ($property['bathrooms']): ?><div><span>Санвузли</span><strong><?php echo $h($property['bathrooms']); ?></strong></div><?php endif; ?>
          <?php if ($property['floor'] || $property['floors']): ?><div><span>Поверх</span><strong><?php echo $h($property['floor'] ?: '-'); ?> / <?php echo $h($property['floors'] ?: '-'); ?></strong></div><?php endif; ?>
        </div>

        <h2>Опис</h2>
        <p><?php echo nl2br($h($property['description'])); ?></p>

        <?php if ($features): ?>
          <h2>Характеристики</h2>
          <div class="tn-feature-list">
            <?php foreach ($features as $feature): ?>
              <span><?php echo $h($feature['feature_key']); ?>: <?php echo $h($feature['feature_value']); ?></span>
            <?php endforeach; ?>
          </div>
        <?php endif; ?>

        <section class="tn-tour-placeholder" id="tour">
          <div>
            <p class="tn-kicker">3D-простір</p>
            <h2>Плейсхолдер для туру або інтерактивної презентації</h2>
            <p>Сюди підключимо Matterport, власний 3D-тур, відео або інтерактивний план, коли буде готовий медіапакет об'єкта.</p>
          </div>
          <?php if ($property['tour_url']): ?>
            <a class="tn-btn tn-btn--accent" href="<?php echo $h($property['tour_url']); ?>">Відкрити тур</a>
          <?php else: ?>
            <span class="tn-tour-placeholder__badge">3D pending</span>
          <?php endif; ?>
        </section>
      </article>

      <aside class="tn-agent-card">
        <p class="tn-kicker">Контакт</p>
        <h2><?php echo $h($property['agent_name'] ?: 'Terra Nova CLUB'); ?></h2>
        <p><?php echo $h($property['agent_bio'] ?: 'Супровід заявки, консультація і передача в CRM.'); ?></p>
        <?php if ($property['agent_phone']): ?><a href="tel:<?php echo $h($property['agent_phone']); ?>"><?php echo $h($property['agent_phone']); ?></a><?php endif; ?>
        <?php if ($property['agent_email']): ?><a href="mailto:<?php echo $h($property['agent_email']); ?>"><?php echo $h($property['agent_email']); ?></a><?php endif; ?>

        <form class="tn-object-form" action="#lead" method="post" id="lead">
          <input type="hidden" name="property_id" value="<?php echo $h($property['id']); ?>">
          <label><span>Ім'я</span><input name="full_name" type="text" autocomplete="name" required></label>
          <label><span>Телефон</span><input name="phone" type="tel" autocomplete="tel"></label>
          <label><span>Email</span><input name="email" type="email" autocomplete="email"></label>
          <label><span>Повідомлення</span><textarea name="message" rows="4" placeholder="Хочу переглянути об'єкт або отримати деталі"></textarea></label>
          <button class="tn-btn tn-btn--accent" type="submit">Надіслати запит</button>
          <?php if ($leadStatus): ?><p class="tn-form-status is-visible"><?php echo $h($leadStatus); ?></p><?php endif; ?>
        </form>
      </aside>
    </section>
  <?php endif; ?>
</main>

<nav class="tn-mobile-dock" aria-label="Швидкі дії">
  <a href="<?php echo $this->url->get('property/catalog'); ?>">Каталог</a>
  <a href="#tour">3D</a>
  <a href="#lead">Запит</a>
</nav>

<footer class="tn-footer">
  <div>
    <strong>Terra Nova CLUB</strong>
    <p>Сторінка об'єкта з підготовкою під CRM, 3D і партнерські продажі.</p>
  </div>
  <div class="tn-footer__links">
    <a href="<?php echo $this->url->get(); ?>">Головна</a>
    <a href="<?php echo $this->url->get('property/catalog'); ?>">Каталог</a>
    <a href="#lead">Контакти</a>
  </div>
</footer>
