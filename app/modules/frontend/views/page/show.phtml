<?php
$h = static fn($value) => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$page = $page ?? [];
$inboundRequestStatus = $inboundRequestStatus ?? null;
?>

<?php $this->partial('shared/public_header', [
    'active' => $page['active'] ?? '',
    'actions' => [
        ['href' => 'property/catalog', 'label' => 'Каталог', 'class' => 'tn-btn--ghost'],
        ['href' => 'contacts', 'label' => 'Залишити запит', 'class' => 'tn-btn--dark'],
    ],
]); ?>

<main class="tn-page tn-public-page">
  <nav class="tn-breadcrumbs" aria-label="Breadcrumb">
    <a href="<?php echo $this->url->get(); ?>">Головна</a><span>/</span><span><?php echo $h($page['kicker'] ?? 'Terra Nova'); ?></span>
  </nav>

  <section class="tn-page-hero">
    <div>
      <p class="tn-kicker"><?php echo $h($page['kicker'] ?? 'Terra Nova'); ?></p>
      <h1><?php echo $h($page['title'] ?? 'Terra Nova CLUB'); ?></h1>
      <p><?php echo $h($page['description'] ?? ''); ?></p>
      <div class="tn-page-hero__actions">
        <a class="tn-btn tn-btn--accent" href="<?php echo $this->url->get('property/catalog'); ?>">Переглянути об’єкти</a>
        <a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('property/submit'); ?>">Подати об’єкт</a>
      </div>
    </div>
  </section>

  <section class="tn-public-page__sections">
    <?php foreach (($page['sections'] ?? []) as $section): ?>
      <article>
        <h2><?php echo $h($section['title']); ?></h2>
        <p><?php echo $h($section['text']); ?></p>
      </article>
    <?php endforeach; ?>
  </section>

  <?php if (!empty($page['has_form'])): ?>
    <section class="tn-public-contact" id="request">
      <div>
        <p class="tn-kicker">Заявка</p>
        <h2>Що потрібно зробити?</h2>
        <p>Залиште контакт і короткий контекст. Запит потрапить у CRM та буде призначений менеджеру.</p>
      </div>
      <form class="tn-object-form" action="<?php echo $this->url->get('contacts'); ?>#request" method="post">
        <input type="hidden" name="request_intent" value="general_contact">
        <input type="hidden" name="role" value="buyer">
        <input type="hidden" name="deal_type" value="consultation">
        <label class="tn-hp" aria-hidden="true"><span>Website</span><input name="website" type="text" tabindex="-1" autocomplete="off"></label>
        <label><span>Ім’я</span><input name="full_name" type="text" autocomplete="name" required></label>
        <label><span>Телефон</span><input name="phone" type="tel" autocomplete="tel"></label>
        <label><span>Email</span><input name="email" type="email" autocomplete="email"></label>
        <label><span>Повідомлення</span><textarea name="message" rows="5" required></textarea></label>
        <button class="tn-btn tn-btn--accent" type="submit">Надіслати</button>
        <?php if ($inboundRequestStatus): ?><p class="tn-form-status is-visible"><?php echo $h($inboundRequestStatus); ?></p><?php endif; ?>
      </form>
    </section>
  <?php else: ?>
    <section class="tn-sales-cta">
      <div><p class="tn-kicker">Наступний крок</p><h2>Перейдіть до конкретної пропозиції</h2></div>
      <div class="tn-hero-actions">
        <a class="tn-btn tn-btn--accent" href="<?php echo $this->url->get('property/catalog'); ?>">Каталог</a>
        <a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('contacts'); ?>">Зв’язатися</a>
      </div>
    </section>
  <?php endif; ?>
</main>

<?php $this->partial('shared/public_footer'); ?>
