<?php
$h = static fn($value) => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$landing = $landing ?? [];
$schema = !empty($landing['schema_json']) ? json_decode((string) $landing['schema_json'], true) : [
    '@context' => 'https://schema.org',
    '@type' => 'WebPage',
    'name' => $landing['title'] ?? '',
    'description' => $landing['meta_description'] ?: $landing['excerpt'],
];
?>

<?php $this->partial('shared/public_header', ['active' => 'catalog', 'actions' => [
    ['href' => 'property/catalog', 'label' => 'Каталог', 'variant' => 'ghost'],
    ['href' => 'contacts', 'label' => 'Отримати добірку', 'variant' => 'primary'],
]]); ?>

<main class="tn-page tn-seo-landing">
  <?php $this->partial('components/ui/page_header', [
      'eyebrow' => (string) (($landing['category'] ?? '') ?: 'Нерухомість'),
      'title' => (string) ($landing['title'] ?? ''),
      'description' => (string) ($landing['excerpt'] ?? ''),
      'actions' => [
          ['label' => 'Переглянути об’єкти', 'href' => 'property/catalog', 'variant' => 'primary'],
          ['label' => 'Підібрати варіанти', 'href' => 'contacts', 'variant' => 'ghost'],
      ],
  ]); ?>

  <?php if (!empty($landing['featured_image_url'])): ?>
    <figure class="tn-seo-landing__media">
      <img src="<?php echo $h($landing['featured_image_url']); ?>" alt="<?php echo $h($landing['featured_image_alt'] ?: $landing['title']); ?>">
    </figure>
  <?php endif; ?>

  <article class="tn-article">
    <div class="tn-article__body"><?php echo $landing['body_html']; ?></div>
  </article>

  <section class="tn-ui-panel tn-workspace-section">
    <div class="tn-ui-panel__head">
      <div>
        <p class="tn-ui-eyebrow">Запит</p>
        <h2>Отримайте актуальну добірку від менеджера</h2>
      </div>
    </div>
    <div class="tn-ui-panel__body">
      <?php $this->partial('components/ui/action_bar', [
          'actions' => [[
              'label' => 'Залишити контакт',
              'href' => 'contacts',
              'variant' => 'primary',
          ]],
      ]); ?>
    </div>
  </section>
</main>

<script type="application/ld+json"><?php echo json_encode($schema, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES); ?></script>
<?php $this->partial('shared/public_footer'); ?>
