<?php
$h = static fn($value) => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$field = static fn($data, $name) => htmlspecialchars((string) ($data[$name] ?? ''), ENT_QUOTES, 'UTF-8');
$formData = $formData ?? [];
$authStatus = $authStatus ?? null;
?>

<?php $this->partial('shared/public_header', ['active' => '', 'actions' => [['href' => 'auth/register', 'label' => 'Реєстрація', 'class' => 'tn-btn--ghost']]]); ?>

<main class="tn-page">
  <section class="tn-auth-layout">
    <div class="tn-auth-copy">
      <?php $this->partial('components/ui/page_header', [
        'eyebrow' => 'Terra Nova CLUB',
        'title' => 'Вхід у кабінет',
        'description' => 'Кабінет зберігає ваші подані об’єкти, заявки, майбутні добірки, перегляди й персональні сценарії Terra Nova.',
      ]); ?>
    </div>
    <form class="tn-auth-form" action="<?php echo $this->url->get('auth/login'); ?>" method="post">
      <label><span>Email</span><input name="email" type="email" autocomplete="email" value="<?php echo $field($formData, 'email'); ?>" required></label>
      <label><span>Пароль</span><input name="password" type="password" autocomplete="current-password" required></label>
      <button class="tn-ui-button tn-ui-button--primary" type="submit">Увійти</button>
      <?php if ($authStatus): ?><div class="tn-ui-alert tn-ui-alert--danger" role="alert"><?php echo $h($authStatus); ?></div><?php endif; ?>
      <p class="tn-auth-form__note">Ще немає акаунта? <a href="<?php echo $this->url->get('auth/register'); ?>">Зареєструватися</a></p>
    </form>
  </section>
</main>
