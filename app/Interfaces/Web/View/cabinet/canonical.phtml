<?php
$h = static fn($value) => htmlspecialchars((string) $value, ENT_QUOTES, 'UTF-8');
$role = (string) ($portalRole ?? 'user');
?>
<main class="tn-page tn-portal-page">
  <section class="tn-portal-hero">
    <div>
      <p class="tn-kicker">COS Portal</p>
      <h1>Кабінет</h1>
      <p>Ваш акаунт уже працює через native Symfony/COS identity та session lifecycle.</p>
    </div>
  </section>

  <section class="tn-portal-section">
    <h2>Профіль</h2>
    <dl class="tn-portal-profile">
      <div><dt>User ID</dt><dd><?php echo $h($currentUser['id'] ?? '—'); ?></dd></div>
      <div><dt>Роль</dt><dd><?php echo $h($role); ?></dd></div>
      <div><dt>Організація</dt><dd><?php echo $h($organizationId ?? '—'); ?></dd></div>
    </dl>
    <div class="tn-actions">
      <a class="tn-btn tn-btn--ghost" href="/">На головну</a>
      <a class="tn-btn tn-btn--ghost" href="/auth/logout">Вийти</a>
    </div>
  </section>
</main>
