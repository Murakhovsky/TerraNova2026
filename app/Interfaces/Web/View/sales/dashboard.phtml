<?php
$h = fn($v) => htmlspecialchars((string) $v, ENT_QUOTES, 'UTF-8');
$workspace = $workspace ?? [];
$kpis = $workspace['kpis'] ?? [];
$today = $workspace['today'] ?? [];
$money = fn($v) => number_format((float) $v, 0, '.', ' ') . ' USD';
?>
<?php $this->partial('shared/manager_header', ['active' => 'sales']); ?>
<main class="tn-page tn-admin-page tn-sales-workspace">
  <section class="tn-listing-hero"><div><p class="tn-kicker">Sales workspace</p><h1>Продажі під контролем COS</h1><p>Пріоритети, ризики й наступні дії — в одному операційному екрані.</p></div><div><a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('sales/leads'); ?>">Leads</a> <a class="tn-btn tn-btn--dark" href="<?php echo $this->url->get('sales/today'); ?>">Відкрити Today</a></div></section>
  <?php if ($pageStatus ?? null): ?><p class="tn-form-status is-visible"><?php echo $h($pageStatus); ?></p><?php endif; ?>
  <section class="tn-sales-kpis">
    <?php foreach ([['Активні угоди',$kpis['active_deals']??0],['Pipeline value',$money($kpis['pipeline_value']??0)],['Expected revenue',$money($kpis['expected_revenue']??0)],['У ризику',$kpis['deals_at_risk']??0],['Follow-ups overdue',$kpis['followups_overdue']??0]] as [$label,$value]): ?>
      <article><span><?php echo $h($label); ?></span><strong><?php echo $h($value); ?></strong></article>
    <?php endforeach; ?>
  </section>
  <div class="tn-sales-columns">
    <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Focus</p><h2>At-risk Deals</h2></div><a href="<?php echo $this->url->get('sales/pipeline?risk=high'); ?>">Усі</a></div>
      <?php foreach ($workspace['at_risk'] ?? [] as $deal): ?><a class="tn-sales-row" href="<?php echo $this->url->get('sales/deals/' . (int)$deal['id']); ?>"><span><strong><?php echo $h($deal['customer']); ?></strong><small><?php echo $h($deal['stage_name'] . ' · ' . $deal['public_id']); ?></small></span><b><?php echo $h($money($deal['deal_value'])); ?></b><em>Risk <?php echo $h($deal['risk_level']); ?></em></a><?php endforeach; ?>
      <?php if (empty($workspace['at_risk'])): ?><p class="tn-muted">Критичних угод немає.</p><?php endif; ?>
    </section>
    <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Inbox</p><h2>Сьогодні</h2></div><a href="<?php echo $this->url->get('sales/today'); ?>">Operational inbox</a></div>
      <?php foreach (array_slice(array_merge($today['overdue'] ?? [], $today['must_do'] ?? []), 0, 8) as $item): ?><a class="tn-sales-row" href="<?php echo $this->url->get('sales/deals/' . (int)$item['id']); ?>"><span><strong><?php echo $h($item['customer']); ?></strong><small><?php echo $h($item['title']); ?></small></span><time><?php echo $h($item['next_contact_at'] ?? 'Без дати'); ?></time></a><?php endforeach; ?>
      <?php if (empty($today['overdue']) && empty($today['must_do'])): ?><p class="tn-muted">На сьогодні немає запланованих дій.</p><?php endif; ?>
    </section>
  </div>
  <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Fresh demand</p><h2>New Leads</h2></div><a href="<?php echo $this->url->get('sales/leads'); ?>">Вхідні заявки</a></div>
    <div class="tn-table-wrap"><table class="tn-listing-table"><thead><tr><th>Lead</th><th>Source</th><th>Status</th><th>Owner</th><th>Next action</th></tr></thead><tbody><?php foreach ($workspace['new_leads'] ?? [] as $lead): ?><tr><td><strong><?php echo $h($lead['name']); ?></strong></td><td><?php echo $h($lead['source']); ?></td><td><?php echo $h($lead['status']); ?></td><td><?php echo $h($lead['owner_name'] ?: 'Unassigned'); ?></td><td><?php echo $h($lead['next_action_at'] ?: '—'); ?></td></tr><?php endforeach; ?></tbody></table></div>
  </section>
  <nav class="tn-sales-tabs"><a href="<?php echo $this->url->get('sales/pipeline'); ?>">Pipeline</a><a href="<?php echo $this->url->get('sales/director'); ?>">Director</a><a href="<?php echo $this->url->get('sales/admin'); ?>">Sales Admin</a></nav>
</main>
