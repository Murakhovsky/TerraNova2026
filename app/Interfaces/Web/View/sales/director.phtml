<?php
$h = fn($v) => htmlspecialchars((string) $v, ENT_QUOTES, 'UTF-8');
$workspace = $workspace ?? [];
$metrics = $workspace['metrics'] ?? [];
$kpis = $workspace['dashboard']['kpis'] ?? [];
$money = fn($v) => number_format((float) $v, 0, '.', ' ') . ' USD';
?>
<?php $this->partial('shared/manager_header', ['active' => 'sales']); ?>
<main class="tn-page tn-admin-page tn-sales-workspace">
  <section class="tn-listing-hero"><div><p class="tn-kicker">Sales · Director</p><h1>Комерційна картина</h1><p>Pipeline, ризик, conversion та вплив COS мовою бізнесу, а не логів.</p></div><a class="tn-btn tn-btn--dark" href="<?php echo $this->url->get('sales/pipeline'); ?>">Pipeline</a></section>
  <?php if ($pageStatus ?? null): ?><p class="tn-form-status is-visible"><?php echo $h($pageStatus); ?></p><?php endif; ?>
  <section class="tn-sales-kpis">
    <article><span>Pipeline value</span><strong><?php echo $h($money($kpis['pipeline_value'] ?? 0)); ?></strong></article>
    <article><span>Expected revenue</span><strong><?php echo $h($money($kpis['expected_revenue'] ?? 0)); ?></strong></article>
    <article><span>Won rate</span><strong><?php echo $h(($metrics['won_rate'] ?? 0) . '%'); ?></strong></article>
    <article><span>Deals at risk</span><strong><?php echo $h($kpis['deals_at_risk'] ?? 0); ?></strong></article>
    <article><span>Sales cycle</span><strong><?php echo $h(($metrics['sales_cycle_hours'] ?? 0) . ' h'); ?></strong></article>
  </section>
  <div class="tn-sales-columns">
    <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Execution</p><h2>COS Impact</h2></div></div><div class="tn-sales-kpis"><article><span>Proposed</span><strong><?php echo $h($metrics['actions_proposed'] ?? 0); ?></strong></article><article><span>Executed</span><strong><?php echo $h($metrics['actions_executed'] ?? 0); ?></strong></article><article><span>Measured outcomes</span><strong><?php echo $h($metrics['actions_successful'] ?? 0); ?></strong></article></div><p class="tn-muted">Revenue influenced не вигадується: показник має зʼявитись тільки після Business Outcome attribution.</p></section>
    <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Discipline</p><h2>Sales process</h2></div></div><p><strong>Follow-up completion:</strong> <?php echo $h(($metrics['followup_completion_rate'] ?? 0) . '%'); ?></p><p><strong>Lost deals:</strong> <?php echo $h($metrics['lost_deals'] ?? 0); ?></p><p><strong>Total deals / 30d:</strong> <?php echo $h($metrics['total_deals'] ?? 0); ?></p></section>
  </div>
  <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Portfolio</p><h2>Останні угоди</h2></div></div><div class="tn-table-wrap"><table class="tn-listing-table"><thead><tr><th>Deal</th><th>Customer</th><th>Stage</th><th>Owner</th><th>Value</th><th>Risk</th></tr></thead><tbody><?php foreach ($workspace['deals'] ?? [] as $deal): ?><tr><td><a href="<?php echo $this->url->get('sales/deals/' . (int)$deal['id']); ?>"><?php echo $h($deal['public_id']); ?></a></td><td><?php echo $h($deal['customer']); ?></td><td><?php echo $h($deal['stage_name']); ?></td><td><?php echo $h($deal['owner_name'] ?: 'Unassigned'); ?></td><td><?php echo $h($money($deal['deal_value'])); ?></td><td><?php echo $h($deal['risk_level']); ?></td></tr><?php endforeach; ?></tbody></table></div></section>
</main>
