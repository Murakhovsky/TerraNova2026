<?php
$h = fn($v) => htmlspecialchars((string) $v, ENT_QUOTES, 'UTF-8');
$workspace = $workspace ?? [];
$pipelines = $workspace['pipelines'] ?? [];
?>
<?php $this->partial('shared/manager_header', ['active' => 'sales']); ?>
<main class="tn-page tn-admin-page tn-sales-workspace">
  <section class="tn-listing-hero"><div><p class="tn-kicker">Sales · Administration</p><h1>Налаштування Sales Domain</h1><p>Бізнес-конфігурація Sales окремо від технічної телеметрії Kernel.</p></div><a class="tn-btn tn-btn--dark" href="<?php echo $this->url->get('cos/control-center'); ?>">COS Control Center</a></section>
  <?php if ($pageStatus ?? null): ?><p class="tn-form-status is-visible"><?php echo $h($pageStatus); ?></p><?php endif; ?>
  <div class="tn-sales-columns">
    <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Pipelines</p><h2>Воронки та етапи</h2></div></div>
      <?php foreach ($pipelines as $pipeline): ?><article class="tn-sales-admin-block"><strong><?php echo $h($pipeline['name'] ?? $pipeline['code'] ?? 'Pipeline'); ?></strong><small><?php echo !empty($pipeline['is_default']) ? 'Default' : ''; ?></small><ol><?php foreach ($pipeline['stages'] ?? [] as $stage): ?><li><span><?php echo $h($stage['name'] ?? $stage['code']); ?></span> <small><?php echo $h($stage['code']); ?> · <?php echo $h($stage['probability_default'] ?? 0); ?>%</small></li><?php endforeach; ?></ol></article><?php endforeach; ?>
      <?php if (empty($pipelines)): ?><p class="tn-muted">Активних Pipeline немає.</p><?php endif; ?>
    </section>
    <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Automation</p><h2>Rules · Agents · Policies</h2></div></div>
      <p>Sales Admin є бізнесовою поверхнею. Runtime-конфігурація та аудит залишаються в generic COS.</p>
      <p><a href="<?php echo $this->url->get('api/cos/rules'); ?>">Rules API</a> · <a href="<?php echo $this->url->get('api/cos/agents'); ?>">Agents API</a> · <a href="<?php echo $this->url->get('api/cos/actions'); ?>">Actions API</a> · <a href="<?php echo $this->url->get('api/cos/audit'); ?>">Audit API</a></p>
      <p class="tn-muted">Policy semantics V0.4: ручна авторизована Sales-операція виконується напряму; AI/COS Action завжди проходить Kernel Policy. APPROVAL_REQUIRED потребує Kernel Approval, HUMAN_ONLY блокує автоматичне виконання.</p>
    </section>
  </div>
  <section class="tn-admin-panel"><div class="tn-section-heading"><div><p class="tn-kicker">Next configuration layer</p><h2>Керування без deploy</h2></div></div><div class="tn-sales-kpis"><article><span>Pipelines</span><strong>Read-ready</strong></article><article><span>Rules</span><strong>Runtime API</strong></article><article><span>Agents</span><strong>Runtime API</strong></article><article><span>Policies</span><strong>Kernel owned</strong></article></div></section>
</main>
