<?php
$h = static fn($v) => htmlspecialchars((string) $v, ENT_QUOTES, 'UTF-8');
$workspace = $workspace ?? [];
$pipelines = $workspace['pipelines'] ?? [];
$metrics = $workspace['metrics'] ?? [];
?>
<main class="tn-page tn-workspace-page tn-sales-workspace" data-sales-workspace>
  <?php $this->partial('components/sales/navigation', ['active' => 'sales-admin']); ?>
  <?php $this->partial('components/ui/page_header', [
    'eyebrow' => 'Sales · Administration',
    'title' => 'Налаштування Sales Domain',
    'description' => 'Бізнес-конфігурація Sales окремо від технічної телеметрії Kernel і generic COS runtime.',
    'actions' => [
      ['label' => 'Configure pipelines', 'href' => 'sales/admin/pipelines', 'variant' => 'primary'],
      ['label' => 'COS Control Center', 'href' => 'cos/control-center', 'variant' => 'ghost'],
    ],
  ]); ?>
  <?php if ($pageStatus ?? null): ?><?php $this->partial('components/ui/state', ['kind' => 'error', 'title' => 'Sales Admin недоступний', 'message' => $pageStatus]); ?><?php endif; ?>
  <div class="tn-ui-split tn-workspace-section">
    <section class="tn-ui-panel"><div class="tn-ui-panel__head"><div><p class="tn-ui-eyebrow">Pipelines</p><h2>Воронки та етапи</h2></div><span><?php echo count($pipelines); ?></span></div><div class="tn-sales-admin-pipelines"><?php foreach ($pipelines as $pipeline): ?><article><header><strong><?php echo $h($pipeline['name'] ?? $pipeline['code'] ?? 'Pipeline'); ?></strong><?php if(!empty($pipeline['is_default'])): ?><span>Default</span><?php endif; ?></header><ol><?php foreach ($pipeline['stages'] ?? [] as $stage): ?><li><span><?php echo $h($stage['name'] ?? $stage['code']); ?></span><small><?php echo $h($stage['code']); ?> · <?php echo $h($stage['probability_default'] ?? 0); ?>%</small></li><?php endforeach; ?></ol></article><?php endforeach; ?><?php if(!$pipelines): ?><p class="tn-sales-empty">Активних Pipeline немає.</p><?php endif; ?></div></section>
    <section class="tn-ui-panel"><div class="tn-ui-panel__head"><div><p class="tn-ui-eyebrow">Automation boundary</p><h2>Rules · Agents · Policies</h2></div></div><p>Sales Admin показує бізнесову конфігурацію. Runtime policies, approvals, audit і agent execution залишаються у generic COS.</p><div class="tn-ui-actions"><a class="tn-ui-button" href="<?php echo $this->url->get('cos/control-center#rules'); ?>">Rules</a><a class="tn-ui-button" href="<?php echo $this->url->get('cos/control-center#agents'); ?>">Agents</a><a class="tn-ui-button" href="<?php echo $this->url->get('cos/control-center#policies'); ?>">Policies</a></div><p class="tn-sales-note">Sales V0.7 semantics: ручна авторизована операція виконується через Sales use case; AI/COS Action проходить Kernel Policy. Pipeline topology тепер адмініструється окремо і версіонується.</p></section>
  </div>
  <section class="tn-ui-kpi-grid tn-workspace-section"><?php $this->partial('components/ui/kpi_card',['label'=>'Pipelines','value'=>count($pipelines),'hint'=>'Configured runtime']); ?><?php $this->partial('components/ui/kpi_card',['label'=>'Won rate / 30d','value'=>($metrics['won_rate']??0).'%']); ?><?php $this->partial('components/ui/kpi_card',['label'=>'Follow-up completion','value'=>($metrics['followup_completion_rate']??0).'%']); ?><?php $this->partial('components/ui/kpi_card',['label'=>'Policies','value'=>'Kernel owned','hint'=>'No duplicate Sales policy layer']); ?></section>
</main>
