<?php
$h = fn($v) => htmlspecialchars((string) $v, ENT_QUOTES, 'UTF-8');
$workspace = $workspace ?? [];
$sections=[
  'must_do'=>'Must Do',
  'ai_recommended'=>'AI Recommended',
  'overdue'=>'Overdue',
  'new_replies'=>'New Replies',
  'meetings'=>'Meetings',
  'followups'=>'Follow-ups',
  'waiting_for_client'=>'Waiting for Client',
];
?>
<?php $this->partial('shared/manager_header', ['active' => 'today']); ?>
<main class="tn-page tn-admin-page tn-sales-workspace">
  <section class="tn-listing-hero"><div><p class="tn-kicker">Operational inbox</p><h1>Today</h1><p>Тільки те, що потребує рішення або дії зараз.</p></div><div><a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('sales/leads'); ?>">Leads</a> <a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('sales/pipeline'); ?>">Pipeline</a></div></section>
  <?php if ($pageStatus ?? null): ?><p class="tn-form-status is-visible"><?php echo $h($pageStatus); ?></p><?php endif; ?>
  <div class="tn-sales-today">
    <?php foreach ($sections as $key=>$label): ?><section class="tn-admin-panel"><div class="tn-section-heading"><h2><?php echo $h($label); ?></h2><span><?php echo count($workspace[$key]??[]); ?></span></div>
      <?php foreach ($workspace[$key]??[] as $item): $dealId=(int)($item['id']??$item['deal_id']??0); ?>
        <?php if ($dealId > 0): ?><a class="tn-sales-row" href="<?php echo $this->url->get('sales/deals/'.$dealId); ?>"><?php else: ?><div class="tn-sales-row"><?php endif; ?>
          <span><strong><?php echo $h($item['customer']??$item['type']??'Action'); ?></strong><small><?php echo $h($item['activity_title']??$item['title']??$item['reason']??$item['detail']??$item['status']??''); ?></small></span>
          <em><?php echo $h($item['risk_level']??$item['priority']??''); ?></em>
        <?php if ($dealId > 0): ?></a><?php else: ?></div><?php endif; ?>
      <?php endforeach; ?>
      <?php if (empty($workspace[$key])): ?><p class="tn-muted">Немає елементів.</p><?php endif; ?>
    </section><?php endforeach; ?>
  </div>
</main>
