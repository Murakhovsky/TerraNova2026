<?php
$h = fn($v) => htmlspecialchars((string) $v, ENT_QUOTES, 'UTF-8');
$workspace = $workspace ?? [];
$sections=['must_do'=>'Must Do','ai_recommended'=>'AI Recommended','overdue'=>'Overdue','waiting_for_client'=>'Waiting for Client'];
?>
<?php $this->partial('shared/manager_header', ['active' => 'today']); ?>
<main class="tn-page tn-admin-page tn-sales-workspace">
  <section class="tn-listing-hero"><div><p class="tn-kicker">Operational inbox</p><h1>Today</h1><p>Тільки те, що потребує рішення або дії зараз.</p></div><a class="tn-btn tn-btn--ghost" href="<?php echo $this->url->get('sales/pipeline'); ?>">Pipeline</a></section>
  <?php if ($pageStatus ?? null): ?><p class="tn-form-status is-visible"><?php echo $h($pageStatus); ?></p><?php endif; ?>
  <div class="tn-sales-today">
    <?php foreach ($sections as $key=>$label): ?><section class="tn-admin-panel"><div class="tn-section-heading"><h2><?php echo $h($label); ?></h2><span><?php echo count($workspace[$key]??[]); ?></span></div>
      <?php foreach ($workspace[$key]??[] as $item): $dealId=(int)($item['id']??$item['deal_id']??0); ?><a class="tn-sales-row" href="<?php echo $this->url->get('client-case/show/'.$dealId); ?>"><span><strong><?php echo $h($item['customer']??$item['type']??'Action'); ?></strong><small><?php echo $h($item['title']??$item['reason']??$item['status']??''); ?></small></span><em><?php echo $h($item['risk_level']??$item['priority']??''); ?></em></a><?php endforeach; ?>
      <?php if (empty($workspace[$key])): ?><p class="tn-muted">Немає елементів.</p><?php endif; ?>
    </section><?php endforeach; ?>
  </div>
</main>
