<div class="cos-workspace-extension">
    <ul class="cos-workspace-extension__list">
        <li>Activity projection is scoped to {{ workspace.entityKey() ?: workspace.definition.id }}.</li>
        <li>Product read models can replace this reference contribution without changing Workspace composition.</li>
    </ul>
</div>
