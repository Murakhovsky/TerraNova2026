<article class="cos-agent-evidence">
    <strong>{{ evidence.label }}</strong>
    {% if evidence.value is iterable %}
        <pre>{{ evidence.value|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
    {% else %}
        <p>{{ evidence.value }}</p>
    {% endif %}
</article>
