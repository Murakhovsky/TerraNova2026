<section id="{{ id }}" class="cos-workspace-panel cos-workspace-panel--context" aria-labelledby="{{ id }}-title">
    <header class="cos-workspace-panel__header">
        <h2 id="{{ id }}-title">{{ title }}</h2>
    </header>
    <div class="cos-workspace-panel__body">{% block content %}{% endblock %}</div>
</section>
