<div class="cos-metric">
    <span class="cos-metric__label">{{ label }}</span>
    <strong class="cos-metric__value">{{ value }}</strong>
    {% if hint %}<small class="cos-metric__hint">{{ hint }}</small>{% endif %}
</div>
