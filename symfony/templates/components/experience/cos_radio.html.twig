<label class="cos-choice">
    <input type="radio" name="{{ name }}" value="{{ value }}" {% if checked %}checked{% endif %} {% if disabled %}disabled{% endif %}>
    <span>{{ label }}</span>
</label>
