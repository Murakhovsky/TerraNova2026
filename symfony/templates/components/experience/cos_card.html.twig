<section class="cos-card{% if raised %} cos-card--raised{% endif %}">
    {% if eyebrow %}<p class="cos-card__eyebrow">{{ eyebrow }}</p>{% endif %}
    <h3 class="cos-card__title">{{ title }}</h3>
    {% if copy %}<p class="cos-card__copy">{{ copy }}</p>{% endif %}
</section>
