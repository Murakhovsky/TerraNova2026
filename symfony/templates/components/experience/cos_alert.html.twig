<section class="{{ this.cssClass() }}" role="status">
    <twig:CosIcon name="{{ this.icon() }}" size="1.15em" />
    <div>
        <h3 class="cos-alert__title">{{ title }}</h3>
        <p class="cos-alert__copy">{{ copy }}</p>
    </div>
</section>
