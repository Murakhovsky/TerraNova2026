<label class="cos-switch">
    <input type="checkbox" name="{{ name }}" {% if checked %}checked{% endif %} {% if disabled %}disabled{% endif %}>
    <span class="cos-switch__control" aria-hidden="true"></span>
    <span>{{ label }}</span>
</label>
