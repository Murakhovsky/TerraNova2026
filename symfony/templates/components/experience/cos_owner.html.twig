<span class="cos-owner">
    <twig:CosAvatar initials="{{ initials }}" name="{{ name }}" size="sm" />
    <span class="cos-owner__copy">
        <strong>{{ name }}</strong>
        {% if role %}<small>{{ role }}</small>{% endif %}
    </span>
</span>
