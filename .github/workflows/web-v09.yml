name: WEB V0.9 Portal Refinement

on:
  push:
    branches: [COS]
  pull_request:
    branches: [COS]
  workflow_dispatch:

jobs:
  portal-refinement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: composer install --no-interaction --prefer-dist
      - run: npm ci
      - name: Build frontend
        run: npm run build
      - name: WEB V0.9 architecture
        run: |
          php tests/architecture/frontend_assets.php
          php tests/architecture/web_v05_module_navigation.php
          php tests/architecture/web_v08_analytics_workspace.php
          php tests/architecture/web_v09_portal_refinement.php
      - name: Syntax and browser module
        run: |
          php -l app/Interfaces/Web/Controller/CabinetController.php
          php -l tests/architecture/web_v09_portal_refinement.php
          node --check frontend/features/portal/cabinet.js
