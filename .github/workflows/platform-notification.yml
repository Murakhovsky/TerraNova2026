name: Platform Notification Foundation

on:
  push:
    branches: [main, migration/symfony, migration/platform-notification]
    paths:
      - 'app/Platform/Notification/**'
      - 'app/Infrastructure/Platform/Notification/**'
      - 'app/Infrastructure/Platform/Persistence/TableOwnership.php'
      - 'app/migrations/20260923_000088_platform_notification_runtime.sql'
      - 'symfony/config/services.yaml'
      - 'tests/unit/platform_notification_foundation.php'
      - 'tests/unit/platform_notification_runtime.php'
      - 'tests/architecture/platform_notification_boundaries.php'
      - 'tests/architecture/platform_notification_runtime.php'
      - '.github/workflows/platform-notification.yml'
  pull_request:
    branches: [main, migration/symfony, migration/platform-audit]
    paths:
      - 'app/Platform/Notification/**'
      - 'tests/unit/platform_notification_foundation.php'
      - 'tests/architecture/platform_notification_boundaries.php'
      - '.github/workflows/platform-notification.yml'
  workflow_dispatch:

jobs:
  pure-php-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          tools: composer:v2
      - run: composer install --no-interaction --prefer-dist --no-progress
      - run: php tests/architecture/platform_notification_boundaries.php
      - run: php tests/architecture/platform_notification_runtime.php
      - run: php tests/unit/platform_notification_foundation.php
      - run: php tests/unit/platform_notification_runtime.php
      - run: find app/Platform/Notification app/Infrastructure/Platform/Notification -type f -name '*.php' -print0 | xargs -0 -n1 php -l
