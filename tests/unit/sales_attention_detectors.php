<?php
declare(strict_types=1);
$root=dirname(__DIR__,2);require $root.'/vendor/autoload.php';
use Domains\Sales\Application\Contract\SalesAttentionRepositoryInterface;use Domains\Sales\Application\UseCase\{DetectMissedFollowups,NoActivityDetector};use Kernel\Event\{DomainEvent,EventBus};use Kernel\Event\Contract\EventStoreInterface;use Kernel\Transaction\Contract\TransactionManagerInterface;
$events=[];$claims=[];$repository=new class($claims) implements SalesAttentionRepositoryInterface{public function __construct(private array &$claims){}public function inactiveDeals(string $o,DateTimeImmutable $c,int $l):array{return [['id'=>'deal-1','last_activity_at'=>'2026-09-01 00:00:00']];}public function missedFollowups(string $o,DateTimeImmutable $n,int $l):array{return [['id'=>'followup-1','client_case_id'=>'deal-1','due_at'=>'2026-09-05 00:00:00']];}public function claimSignal(string $o,string $t,string $k,string $m):bool{$key="$o:$t:$k";if(isset($this->claims[$key]))return false;$this->claims[$key]=true;return true;}};
$tx=new class implements TransactionManagerInterface{public function transactional(callable $o):mixed{return $o();}public function isActive():bool{return true;}public function afterCommit(callable $c):void{$c();}};
$store=new class($events) implements EventStoreInterface{public function __construct(private array &$events){}public function append(DomainEvent $e):void{$this->events[]=$e;}public function find(string $id):?DomainEvent{return null;}public function findByAggregate(string $o,string $t,string $id,int $l=100):array{return [];}};$bus=new EventBus($store,$tx);$now=new DateTimeImmutable('2026-09-06T12:00:00Z');$inactive=new NoActivityDetector($repository,$bus,$tx);$missed=new DetectMissedFollowups($repository,$bus,$tx);
if($inactive->detect('org-1',$now)!==1||$inactive->detect('org-1',$now)!==0)throw new RuntimeException('NoActivityDetector is not idempotent.');if($missed->detect('org-1',$now)!==1||$missed->detect('org-1',$now)!==0)throw new RuntimeException('Missed follow-up detector is not idempotent.');$types=array_map(fn(DomainEvent $e)=>$e->type,$events);if($types!==['sales.no_activity_detected','sales.followup.missed'])throw new RuntimeException('Attention detector canonical events are incorrect.');echo "Sales attention detector producers passed.\n";
